using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UIEffect
{
    public enum TransitionMode
    {
        Fade = 0, //alpha退去
        Cutoff, //剪切
        Dissolve, //溶解
    }
}